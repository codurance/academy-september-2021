import {FeatureRoute} from "../../../shared/navigation/FeatureRoute";

export class ProfileFeatureRoute {
    public static readonly SEARCH = FeatureRoute.PROFILE;
    public static readonly RESULTS = '/results';
    public static readonly PROFILE = '/profile';
}